#include<iostream>
#include<cmath>

using namespace std;

int main(){
    int num;
    cout << "Enter a number to find its square root: ";
    cin >> num;
    cout << "Square root is " << sqrt(num);
    return 0;
}
